<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.udacity.popularmovies">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Allows the application to keep the screen from dimming or the processor from sleeping -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.udacity.popularmovies.TERMS_READ" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <meta-data android:name="android.webkit.WebView.EnableSafeBrowsing"
            android:value="true" />
        <activity
            android:name=".activities.MovieListActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MovieDetailsActivity"
            android:label="@string/movie_details"
            android:launchMode="singleTop"
            android:parentActivityName=".activities.MovieListActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".activities.BookmarkActivity"
            android:label="@string/bookmarks_offline"
            android:parentActivityName=".activities.MovieListActivity" />



        <activity
            android:name=".activities.MovieVideosActivity"
            android:label="@string/trailers_and_clips"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:parentActivityName=".activities.MovieDetailsActivity" />


        <!-- Bookmark Provider for Database access through ContentProvider class -->
        <provider
            android:name=".localdatabase.BookmarkContentProvider"
            android:authorities="com.udacity.popularmovies"
            android:exported="true" />

        <activity android:name=".activities.Player1Activity" />
        <activity android:name=".activities.Player2Activity" />
        <activity android:name=".activities.Player3Activity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_video_player"
            android:parentActivityName=".activities.MovieVideosActivity"
            android:theme="@style/FullscreenTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.udacity.popularmovies.activities.MovieVideosActivity" />
        </activity>

        <activity android:name=".activities.WebViewActivity"
            android:label="@string/movie_homepage"
            android:parentActivityName=".activities.MovieDetailsActivity"/>
    </application>

</manifest>