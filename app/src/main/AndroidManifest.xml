<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dev.jesselima.jmovies">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Allows the application to keep the screen from dimming or the processor from sleeping -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="dev.jesselima.jmovies.TERMS_READ" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <meta-data android:name="android.webkit.WebView.EnableSafeBrowsing"
            android:value="true" />
        <activity
            android:name="dev.jesselima.jmovies.activities.MovieListActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="dev.jesselima.jmovies.activities.MovieDetailsActivity"
            android:label="@string/movie_details"
            android:launchMode="singleTop"
            android:parentActivityName="dev.jesselima.jmovies.activities.MovieListActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />

        <!--  android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" -->
        <activity
            android:name="dev.jesselima.jmovies.activities.BookmarkActivity"
            android:label="@string/bookmarks"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:parentActivityName="dev.jesselima.jmovies.activities.MovieListActivity"/>

        <activity android:name="dev.jesselima.jmovies.activities.WebViewActivity"
            android:label="@string/movie_homepage"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:parentActivityName="dev.jesselima.jmovies.activities.MovieDetailsActivity"/>

        <activity
            android:name="dev.jesselima.jmovies.activities.SettingsActivity"
            android:label="@string/settings"
            android:parentActivityName="dev.jesselima.jmovies.activities.MovieListActivity">
            <meta-data
                android:name="androidx.PARENT_ACTIVITY"
                android:value="dev.jesselima.jmovies.activities.MovieListActivity" />
        </activity>

        <!-- Bookmark Provider for Database access through ContentProvider class -->
        <provider
            android:name="dev.jesselima.jmovies.localdatabase.BookmarkContentProvider"
            android:authorities="dev.jesselima.jmovies"
            android:exported="false" />

    </application>

</manifest>